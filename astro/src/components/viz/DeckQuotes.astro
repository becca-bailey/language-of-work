---
import DeckQuotes from "@/components/DeckQuotes";
import { loadNetflixStory } from "@/lib/netflixStory";

const n = await loadNetflixStory();
---

{n && (
  <div class="my-6">
    <DeckQuotes quotes={n.deckQuotes} />
  </div>
)}
