---
import NetflixConceptTree from "@/components/NetflixConceptTree";
import { loadNetflixStory } from "@/lib/netflixStory";

const n = await loadNetflixStory();
---

{n && (
  <div class="my-6">
    <NetflixConceptTree concepts={n.propagation.concepts} />
  </div>
)}
