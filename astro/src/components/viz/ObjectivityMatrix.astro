---
import NetflixObjectivityMatrix from "@/components/NetflixObjectivityMatrix";
import { loadNetflixStory } from "@/lib/netflixStory";

const n = await loadNetflixStory();
---

{n && (
  <div class="my-6">
    <NetflixObjectivityMatrix rows={n.objectivityMatrix} />
  </div>
)}
