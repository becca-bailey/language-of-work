---
import ImplicitExplicitTable from "@/components/ImplicitExplicitTable";
import { loadNetflixStory } from "@/lib/netflixStory";

const n = await loadNetflixStory();
---

{n && (
  <div class="my-6">
    <ImplicitExplicitTable rows={n.implicitExplicit} />
  </div>
)}
